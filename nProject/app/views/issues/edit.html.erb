<h2><%=h "#{@issue.tracker.name} ##{@issue.project_issue_id}" %></h2>

<%= render :partial => 'edit' %>
<% content_for :header_tags do %>
    <%= robot_exclusion_tag %>
<% end %>

<%= render 'action_menu_filter' %>